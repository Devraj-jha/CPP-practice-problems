#include <iostream>
using namespace std;
int main(){

//name.lenght()- checks the lenght of strings used in logiccal statements
//name.clear()- delte the name
//name.empty()- checks if the name is empty useful to check whether a user entered a name or not
//std::getline(std::cin, string_variable); - reads the entire line cin only reads a word
//name.append(things to add in the end)- adds the name in the end
//character reading starts form 0 in cpp (devraj - 012345)
//name.insert(0,@)- adds @at 0 position example - @devraj
//name.find("cam be uised to find certain words position")
//name.erase(no. erase from this, to this no. ) - name.erase(0,3) - raj
//more at cpp.com or chatgpt
}
